{
  "name": "the-long-road-of-the-weyfinder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "build:pages": "vite build --base=/TheLongRoadofTheWeyfinder/",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
